spring:
  application:
    name: calculator-service
server:
  port: 8080
math-service:
  ribbon:
    eureka:
      enabled: false
    listOfServers: localhost:8090,localhost:8091
    ServerListRefreshInterval: 15000
